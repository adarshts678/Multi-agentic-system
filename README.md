# Multi-agentic-system

## 1. create virtual env
   ```bash
   python -m venv multi-agent
   ```
## 2. Install required libraries
   ```bash
   pip install langgraph langchain langchain-community langchain-tavily psycopg[binary] psycopg_pool python-dotenv tavily-python requests streamlit
   ```
## 3.configure API keys in .env file

## 4. Install and setup postgres
      ```
      username and password : postgres ,Adarsh@123
      port : 5432

## 5. command to run the application:

   ```bash
   streamlit run ./frontend.py
   ```
